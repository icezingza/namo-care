{
  "name": "namo-care-functions",
  "version": "1.0.0",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf lib",
    "lint": "eslint src --ext .ts",
    "serve": "npm run build",
    "seed:demo": "node scripts/seed-demo.js",
    "seed:clean": "node scripts/clean-demo.js",
    "seed:scenario": "node scripts/seed-scenario.js",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@line/bot-sdk": "^9.7.0",
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.18.6",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.3"
  }
}
